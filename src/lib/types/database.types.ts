// Placeholder types — sẽ được thay bằng file sinh tự động sau Phase 1:
//   npx supabase gen types typescript --project-id <id> > src/lib/types/database.types.ts
// Giữ kiểu `Database = any` tạm thời để không chặn build trước khi có schema thật.
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
